#!/bin/bash

VAGRANT_PATH=/opt/nw-project

case "$1" in
up)
    cd `echo $VAGRANT_PATH` && vagrant $1
;;
status)
    cd `echo $VAGRANT_PATH` && vagrant $1
;;
destroy)
    cd `echo $VAGRANT_PATH` && vagrant $1
;;
*)
    echo "Options: $0 {destroy|status|up}"
    exit 1
esac
